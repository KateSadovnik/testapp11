<p id="notice"><%= notice %></p>

<h1>Microposts1s</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @microposts1s.each do |microposts1| %>
      <tr>
        <td><%= microposts1.content %></td>
        <td><%= microposts1.user_id %></td>
        <td><%= link_to 'Show', microposts1 %></td>
        <td><%= link_to 'Edit', edit_microposts1_path(microposts1) %></td>
        <td><%= link_to 'Destroy', microposts1, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Microposts1', new_microposts1_path %>
